[app]
name = "AgentFlow"
version = "0.1.0"
debug = true
secret_key = "change-me-to-a-random-secret-key"
algorithm = "HS256"
access_token_expire_minutes = 30

[database]
host = "localhost"
port = 3306
user = "root"
password = "root@123"
database = "agentflow"
pool_size = 10
max_overflow = 20
echo = false

[vector_store]
type = "milvus"
dimension = 1024

# llm / embedding / log config lives in .env